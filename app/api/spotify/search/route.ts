import { NextRequest,NextResponse } from "next/server"; import { spotifyFetch } from "@/lib/spotify/client"; import type { SearchResults } from "@/types/spotify"; export async function GET(r:NextRequest){const q=r.nextUrl.searchParams.get("q")?.trim();if(!q||q.length>120)return NextResponse.json({error:"Enter a search of up to 120 characters."},{status:400});try{return NextResponse.json(await spotifyFetch<SearchResults>(`/search?${new URLSearchParams({q,type:"track,artist,album,playlist",limit:"12"})}`));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Search unavailable"},{status:400});}}
