import { NextResponse } from "next/server"; import { accessToken } from "@/lib/spotify/client"; export async function GET(){try{const token=await accessToken();if(!token)return NextResponse.json({error:"Unauthenticated"},{status:401});return NextResponse.json({access_token:token},{headers:{"Cache-Control":"no-store"}});}catch{return NextResponse.json({error:"Session expired"},{status:401});}}
