import { NextRequest, NextResponse } from "next/server"; import { safeId, spotifyFetch } from "@/lib/spotify/client"; import type { SpotifyPlaylistDetail } from "@/types/spotify"; export async function GET(_:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!safeId(id))return NextResponse.json({error:"Invalid playlist."},{status:400});try{return NextResponse.json(await spotifyFetch<SpotifyPlaylistDetail>(`/playlists/${id}?market=from_token`));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Playlist unavailable"},{status:404});}}
