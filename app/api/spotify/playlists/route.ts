import { NextResponse } from "next/server"; import { spotifyFetch } from "@/lib/spotify/client"; import type { SpotifyPlaylist } from "@/types/spotify"; export async function GET(){try{return NextResponse.json(await spotifyFetch<{items:SpotifyPlaylist[]}>("/me/playlists?limit=50"));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to load playlists"},{status:401});}}
