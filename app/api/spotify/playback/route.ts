import { NextRequest,NextResponse } from "next/server"; import { safeId,spotifyFetch } from "@/lib/spotify/client"; export async function PUT(r:NextRequest){try{const b=await r.json() as {uri?:string;position_ms?:number;device_id?:string};if(b.uri&&!/^spotify:track:[A-Za-z0-9]{22}$/.test(b.uri))return NextResponse.json({error:"Invalid track."},{status:400});if(b.device_id&&!safeId(b.device_id))return NextResponse.json({error:"Invalid device."},{status:400});const q=new URLSearchParams();if(b.device_id)q.set("device_id",b.device_id);await spotifyFetch(`/me/player/play?${q}`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({uris:b.uri?[b.uri]:undefined,position_ms:b.position_ms})});return NextResponse.json({ok:true});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Playback unavailable"},{status:400});}}
