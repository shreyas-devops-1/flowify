import { NextResponse } from "next/server"; import { createHash, randomBytes } from "crypto"; import { callbackUrl, SPOTIFY_SCOPES } from "@/lib/config";
export async function GET() { if(!process.env.SPOTIFY_CLIENT_ID) return NextResponse.redirect(new URL("/login?error=configuration",callbackUrl())); const verifier=randomBytes(64).toString("base64url"), state=randomBytes(24).toString("base64url"), challenge=createHash("sha256").update(verifier).digest("base64url"); const url=new URL("https://accounts.spotify.com/authorize"); url.search=new URLSearchParams({client_id:process.env.SPOTIFY_CLIENT_ID,response_type:"code",redirect_uri:callbackUrl(),state,code_challenge_method:"S256",code_challenge:challenge,scope:SPOTIFY_SCOPES.join(" ")}).toString(); const response=NextResponse.redirect(url); const common={httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax" as const,path:"/api/auth/spotify/callback",maxAge:600}; response.cookies.set("flowify_oauth_state",state,common); response.cookies.set("flowify_oauth_verifier",verifier,common); return response; }
